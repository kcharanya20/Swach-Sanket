# JWT Configuration (required for memory storage too)
JWT_SECRET=swach_sanket_memory_jwt_secret_2024
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# Client Configuration
CLIENT_ORIGIN=http://localhost:3000

# Note: MONGO_URI not needed for memory storage
